# casu-last-stand-replacer
The base for the "I didn't hear no bell" mod for Casualties: Unknown.
Uses some (minimal) amounts of code (particularly the csproj) from https://github.com/molten-basalt/CUResprite, but the codebase is mostly different.
## Building
Run `dotnet publish -c Release` once the repo is cloned.
